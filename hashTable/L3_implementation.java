public class L3_implementation {
    public static void main(String args[]){
        
    }
}

// creating fixed size array with each cell contain linked list with next = null;
        // Array of LL
        // Buckets

        // *** n = nodes/ pairs, N=Buckets
        //  "India" ---> hashCode() -----> 1245 (Hash function())

        // key --> hash function --> buckets index

        // put

        // **hashf(x) -> bi
        // **loop ll in the bucket

        // 1. key exist : update
                    
        // 2. key does not exist: add new node in LL

        // why o(1)?
        // N=4  n=7  &(lambda)=n/N 
        // &<=k constant Threshold value

        // if n increase then &<=K 🚫 
        // rehashing -> new array = 2 * old size
        
